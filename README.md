# 🔍 Поисковый движок

Локальный поисковый движок для индексации и поиска по веб-сайтам. Итоговый проект курса «Java-разработчик».

## 📋 Описание

Spring Boot приложение, которое:
- 🕷️ Обходит и индексирует указанные сайты (многопоточный краулинг)
- 📝 Анализирует содержимое страниц с учётом морфологии русского языка (лемматизация)
- 🔎 Обеспечивает быстрый поиск по ключевым словам с ранжированием результатов
- 🌐 Предоставляет веб-интерфейс и REST API для управления
- 💾 Работает с локальной базой данных MySQL

## 🚀 Технологии

- **Backend:** Java 11, Spring Boot 2.7.18
- **Database:** PostgreSQL 14+ (или MySQL 8.0 для обратной совместимости)
- **Migrations:** Liquibase
- **HTML Parser:** JSOUP 1.17.2
- **Build Tool:** Maven
- **Frontend:** HTML, CSS, JavaScript
- **Concurrency:** Fork-Join Framework

## ⚙️ Требования

- JDK 11 или выше
- PostgreSQL 14 или выше (или MySQL 8.0)
- Maven 3.6 или выше

## 🏁 Быстрый старт

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/FrolovArtem/search-engine.git
cd search-engine
```

### Шаг 2: Создание базы данных

**Для PostgreSQL (рекомендуется):**

```bash
psql -U postgres
CREATE DATABASE search_engine;
\q
```

**Или для MySQL:**

```bash
mysql -u root -p < database.sql
```

Или создайте вручную:

```sql
CREATE DATABASE search_engine CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### Шаг 3: Настройка приложения

Отредактируйте `src/main/resources/application.yaml`:

**Для PostgreSQL:**
```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/search_engine
    username: postgres
    password: your_password
```

**Для MySQL:**
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/search_engine
    username: root
    password: your_password
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
```

**⚠️ Важно:** 
- Liquibase автоматически создаст все таблицы при первом запуске
- Для тестирования используйте небольшие сайты!

### Шаг 4: Сборка проекта

```bash
mvn clean package
```

### Шаг 5: Запуск приложения

**Вариант 1 - через JAR:**
```bash
java -jar target/SearchEngine-1.0.0.jar
```

**Вариант 2 - через Maven:**
```bash
mvn spring-boot:run
```

### Шаг 6: Открытие веб-интерфейса

Откройте браузер: **http://localhost:8080**

## 📖 Использование

### Веб-интерфейс

Интерфейс состоит из трёх вкладок:

1. **Dashboard** 📊
   - Общая статистика по всем сайтам
   - Детальная информация по каждому сайту
   - Статус индексации

2. **Management** ⚙️
   - Запуск полной индексации
   - Остановка индексации
   - Индексация отдельной страницы

3. **Search** 🔍
   - Поиск по всем сайтам или конкретному
   - Результаты с релевантностью
   - Сниппеты с выделением совпадений

### REST API

#### 1. Получение статистики
```http
GET /api/statistics
```

**Ответ:**
```json
{
  "result": true,
  "statistics": {
    "total": {
      "sites": 3,
      "pages": 1523,
      "lemmas": 45678,
      "indexing": false
    },
    "detailed": [...]
  }
}
```

#### 2. Запуск индексации
```http
GET /api/startIndexing
```

**Ответ:**
```json
{
  "result": true
}
```

#### 3. Остановка индексации
```http
GET /api/stopIndexing
```

#### 4. Индексация отдельной страницы
```http
POST /api/indexPage?url=https://www.example.com/page
```

#### 5. Поиск
```http
GET /api/search?query=java разработка&site=https://www.example.com&offset=0&limit=20
```

**Параметры:**
- `query` (обязательный) - поисковый запрос
- `site` (опционально) - URL сайта для поиска
- `offset` (опционально, по умолчанию 0) - смещение для пагинации
- `limit` (опционально, по умолчанию 20) - количество результатов

**Ответ:**
```json
{
  "result": true,
  "count": 42,
  "data": [
    {
      "site": "https://www.example.com",
      "siteName": "Example Site",
      "uri": "/page/123",
      "title": "Заголовок страницы",
      "snippet": "Текст с <b>выделенными</b> совпадениями...",
      "relevance": 0.98
    }
  ]
}
```

### Примеры с curl

```bash
# Статистика
curl http://localhost:8080/api/statistics

# Запуск индексации
curl http://localhost:8080/api/startIndexing

# Поиск
curl "http://localhost:8080/api/search?query=java"

# Индексация страницы
curl -X POST "http://localhost:8080/api/indexPage?url=https://www.example.com/page"
```

## 🏗️ Архитектура

### Структура проекта

```
searchengine/
├── config/              # Конфигурация приложения
├── controllers/         # REST API контроллеры
├── dto/                # Data Transfer Objects
├── model/              # Entity классы (JPA)
├── repository/         # JPA репозитории
├── services/           # Бизнес-логика
└── Application.java    # Точка входа
```

### База данных

**4 таблицы:**

1. **site** - информация о сайтах
   - Статус индексации (INDEXING/INDEXED/FAILED)
   - Время последнего обновления
   - Ошибки индексации

2. **page** - проиндексированные страницы
   - Путь страницы
   - HTTP код ответа
   - HTML контент

3. **lemma** - леммы (базовые формы слов)
   - Текст леммы
   - Частота встречаемости

4. **index** - поисковый индекс
   - Связь страница-лемма
   - Ранг (количество вхождений)

### Принцип работы

1. **Индексация:**
   - Многопоточный обход сайта (Fork-Join Framework)
   - Парсинг HTML с помощью JSOUP
   - Извлечение текста и лемматизация
   - Сохранение в поисковый индекс

2. **Поиск:**
   - Лемматизация поискового запроса
   - Поиск страниц, содержащих все леммы
   - Расчет релевантности
   - Формирование сниппетов с выделением
   - Сортировка по релевантности

## 🔧 Решение проблем

### Ошибка подключения к БД
```
Проверьте:
- MySQL запущен
- Логин и пароль в application.yaml
- База данных создана
```

### Port 8080 already in use
```yaml
# Измените порт в application.yaml
server:
  port: 8081
```

### Медленная индексация
```
- Используйте меньшие сайты для тестирования
- Увеличьте задержку между запросами
- Уменьшите количество сайтов
```

### Очистка базы данных
```sql
USE search_engine;
DELETE FROM `index`;
DELETE FROM lemma;
DELETE FROM page;
DELETE FROM site;
```

## 📝 Примечания

### Лемматизация

Текущая версия использует **упрощенную лемматизацию** (удаление окончаний и стоп-слов).

Для продакшена рекомендуется подключить **Apache Lucene Morphology** из репозитория Skillbox для более точной морфологической обработки текста.

### Рекомендации

- ✅ Начинайте с небольших сайтов (10-50 страниц)
- ✅ Проверяйте robots.txt перед индексацией
- ✅ Используйте задержки между запросами
- ❌ Не индексируйте крупные сайты без оптимизации
- ❌ Не запускайте несколько индексаций одновременно

## 🎯 Возможности для улучшения

- [ ] Подключить Apache Lucene Morphology для точной лемматизации
- [ ] Добавить unit и integration тесты
- [x] Реализовать обработку robots.txt
- [ ] Добавить Docker контейнеризацию
- [ ] Улучшить логирование и мониторинг
- [ ] Добавить кэширование результатов поиска
- [ ] Swagger/OpenAPI документация API
- [ ] Обработка JavaScript-контента
- [ ] Инкрементальная индексация

## 🌐 Деплой на Beget

### Требования
- Аккаунт на Beget (14 дней бесплатно)
- VPS тариф (от 200₽/месяц после теста)

### Пошаговая инструкция

**1. Регистрация и создание VPS**
- Зарегистрируйтесь на https://beget.com
- Закажите VPS: Ubuntu 22.04, минимальная конфигурация
- Получите IP адрес и пароль root

**2. Подключение к серверу**
```bash
ssh root@<IP_АДРЕС>
```

**3. Установка зависимостей**
```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Java 11
apt install openjdk-11-jdk -y

# Установка MySQL
apt install mysql-server -y

# Установка Maven
apt install maven -y

# Установка Git
apt install git -y
```

**4. Настройка MySQL**
```bash
mysql
```
```sql
CREATE DATABASE search_engine CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'searchengine'@'localhost' IDENTIFIED BY 'YourStrongPassword123!';
GRANT ALL PRIVILEGES ON search_engine.* TO 'searchengine'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

**5. Клонирование и настройка проекта**
```bash
cd /opt
git clone https://github.com/FrolovArtem/search-engine.git
cd search-engine

# Редактирование конфигурации
nano src/main/resources/application.yaml
```

Измените:
```yaml
spring:
  datasource:
    username: searchengine
    password: YourStrongPassword123!
```

**6. Сборка и запуск**
```bash
# Сборка
mvn clean package -DskipTests

# Создание systemd сервиса
cat > /etc/systemd/system/search-engine.service << 'EOF'
[Unit]
Description=Search Engine Application
After=mysql.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/search-engine
ExecStart=/usr/bin/java -jar /opt/search-engine/target/SearchEngine-1.0.0.jar
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

# Запуск сервиса
systemctl daemon-reload
systemctl enable search-engine
systemctl start search-engine

# Проверка статуса
systemctl status search-engine
```

**7. Открытие порта 8080**

В панели управления Beget:
- Перейдите в раздел "Firewall"
- Добавьте правило: TCP порт 8080, источник 0.0.0.0/0

**8. Готово!**

Откройте в браузере: `http://<IP_АДРЕС>:8080`

### Полезные команды

```bash
# Просмотр логов
journalctl -u search-engine -f

# Перезапуск
systemctl restart search-engine

# Остановка
systemctl stop search-engine

# Обновление после изменений в GitHub
cd /opt/search-engine
git pull
mvn clean package -DskipTests
systemctl restart search-engine
```

## 📄 Лицензия

Итоговый проект курса «Java-разработчик»

## 👤 Автор

Создано в рамках обучения на курсе Skillbox

---

⭐ Если проект был полезен, поставьте звезду на GitHub!
