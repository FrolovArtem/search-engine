# 🚀 Инструкция по деплою на Timeweb Cloud

## Шаг 1: Регистрация на Timeweb Cloud

1. Открой https://timeweb.cloud
2. Нажми **"Попробовать бесплатно"**
3. Зарегистрируйся (email + телефон)
4. Получи 30 дней бесплатного тестового периода

## Шаг 2: Создание облачного сервера

1. В панели управления перейди: **"Серверы"** → **"Создать сервер"**
2. Выбери конфигурацию:
   - **Операционная система:** Ubuntu 22.04 LTS
   - **Тариф:** Минимальный (1 vCPU, 1 GB RAM, 10 GB SSD)
   - **Локация:** Москва или Санкт-Петербург
   - **Имя сервера:** search-engine
3. Нажми **"Заказать"**
4. Дождись создания сервера (1-2 минуты)

## Шаг 3: Получение данных для подключения

После создания сервера в панели управления ты увидишь:
- **IP адрес** (например: 123.45.67.89)
- **Логин:** root
- **Пароль:** (будет отправлен на email или показан в панели)

## Шаг 4: Подключение к серверу

### Вариант А: Через веб-консоль Timeweb (проще)
1. В панели управления нажми на сервер
2. Нажми кнопку **"Консоль"**
3. Введи логин `root` и пароль

### Вариант Б: Через SSH клиент
```bash
ssh root@<IP_АДРЕС>
# Введи пароль
```

## Шаг 5: Автоматический деплой

После подключения к серверу выполни одну команду:

```bash
curl -sSL https://raw.githubusercontent.com/FrolovArtem/search-engine/main/deploy.sh | bash
```

Или вручную:

```bash
# Скачай скрипт
wget https://raw.githubusercontent.com/FrolovArtem/search-engine/main/deploy.sh

# Сделай исполняемым
chmod +x deploy.sh

# Запусти
./deploy.sh
```

Скрипт автоматически:
- ✅ Установит Java 11
- ✅ Установит Maven
- ✅ Установит MySQL
- ✅ Создаст базу данных
- ✅ Склонирует проект
- ✅ Соберет приложение
- ✅ Настроит автозапуск
- ✅ Запустит приложение

## Шаг 6: Открытие порта 8080

1. В панели Timeweb перейди: **"Серверы"** → твой сервер → **"Firewall"**
2. Нажми **"Добавить правило"**
3. Заполни:
   - **Протокол:** TCP
   - **Порт:** 8080
   - **Источник:** Anywhere (0.0.0.0/0)
4. Нажми **"Добавить"**

## Шаг 7: Проверка работы

Открой в браузере:
```
http://<IP_АДРЕС>:8080
```

Должна открыться главная страница поисковика!

## 🔧 Полезные команды

### Проверка статуса приложения
```bash
sudo systemctl status search-engine
```

### Просмотр логов
```bash
sudo journalctl -u search-engine -f
```

### Перезапуск приложения
```bash
sudo systemctl restart search-engine
```

### Остановка приложения
```bash
sudo systemctl stop search-engine
```

### Обновление приложения (после изменений в GitHub)
```bash
cd /opt/search-engine
git pull
mvn clean package -DskipTests
sudo systemctl restart search-engine
```

## 🐛 Решение проблем

### Приложение не запускается
```bash
# Проверь логи
sudo journalctl -u search-engine -n 100

# Проверь, что MySQL работает
sudo systemctl status mysql

# Проверь, что порт 8080 свободен
sudo netstat -tulpn | grep 8080
```

### MySQL ошибки
```bash
# Перезапусти MySQL
sudo systemctl restart mysql

# Проверь подключение
mysql -u searchengine -p
# Пароль: SearchEngine2024!
```

### Нет доступа извне
- Проверь, что порт 8080 открыт в Firewall
- Проверь, что приложение слушает 0.0.0.0, а не 127.0.0.1

## 📊 Мониторинг

### Использование ресурсов
```bash
# CPU и память
htop

# Дисковое пространство
df -h

# Процессы Java
ps aux | grep java
```

## 🔐 Безопасность

После деплоя рекомендуется:

1. **Сменить пароль root:**
```bash
passwd
```

2. **Создать отдельного пользователя:**
```bash
adduser deploy
usermod -aG sudo deploy
```

3. **Настроить SSH ключи** (вместо пароля)

4. **Установить fail2ban:**
```bash
sudo apt install fail2ban -y
```

## 💰 Стоимость

- **Первые 30 дней:** Бесплатно
- **После тестового периода:** ~150-200₽/месяц за минимальную конфигурацию

## 📞 Поддержка

Если возникли проблемы:
- Техподдержка Timeweb: https://timeweb.cloud/support
- Документация: https://timeweb.cloud/help

---

**Готово! Твой поисковый движок работает в облаке!** 🎉
